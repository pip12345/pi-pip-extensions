# pip-common

Shared runtime and testing utilities for the `pi-pip-extensions` packages.

Includes shared `/pip-settings` support and a small pip-owned tool broker/finalizer API used by extensions such as `pi-todo` and `pi-quiet-tools`.
